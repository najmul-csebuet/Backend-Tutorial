version: "3.9"

services:
  db:
    image:
      postgres:latest
    ports:
    - 5432:5432
    container_name: "pg-docker"
    environment:
      POSTGRES_DB: "test_db"
      POSTGRES_USER: "test"
      POSTGRES_PASSWORD: "1234"